# chatbot/train_chatbot.py
from model import model  # This will train the model using model.py's code

if __name__ == "__main__":
    print("Training the chatbot model...")
    # The training is already invoked in model.py
